exports.run = async (client, message, args, admin) => {

	/*

		ONLY OWNER CAN RUN THS COMMAND
	
		check if owner is whitelisted

		check if guild is binded

		if binded then unbind

		if not then dont do anythings

	*/

};

exports.info = {
    name: 'verify',
    usage: 'verify <rblx_username>',
    description: "Link a user's Discord account with their ROBLOX account"
};